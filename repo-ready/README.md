# Repo Split Layout

This folder contains repo-ready exports from the current monorepo.

## Microservice repos

- `auth-service`
- `compliance-service`
- `food-service`
- `macro-service`
- `frontend`

## Shared repos

- `shared-ci-cd`: reusable GitHub Actions workflow templates
- `platform-deployments`: Helm charts, Kubernetes manifests, infrastructure config, and future Argo CD apps

The original source layout is unchanged outside this folder.
