apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.foodService.name }}
  namespace: {{ .Values.global.namespace }}
  labels:
    app: {{ .Values.foodService.name }}
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.foodService.port }}
      targetPort: {{ .Values.foodService.port }}
      protocol: TCP
      name: http
  selector:
    app: {{ .Values.foodService.name }}
