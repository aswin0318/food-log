apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Values.frontend.name }}-config
  namespace: {{ .Values.global.namespace }}
  labels:
    app: {{ .Values.frontend.name }}
data:
  NEXT_PUBLIC_API_URL: "{{ .Values.frontend.apiBaseUrl }}"
  NODE_ENV: "production"
